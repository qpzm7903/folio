# 问题总结 — 未完成工作交接

> 生成时间: 2026-06-28
> 当前版本: v0.21.2+66 (已提交 `66d7ea7`, hap 构建成功, 未装机验收)
> 上一验收版本: v0.21.0 (用户真机确认通过)

---

## 一、v0.21.2 真机验收 (已通过 ✅)

**状态**: 2026-06-28 装机验收通过, 衬线字体已生效。

**已验证**:
1. ✅ Mate 80 已连接 → AGC 签名装机成功
2. ✅ 左下角无「金」朱印
3. ✅ 右下角只有 1 个按钮 (下一句箭头), 无模式切换按钮
4. ✅ 点下一句能换句
5. ✅ 墨色比之前暖 (paper 主题 ink 从 #1d2a1f → #2a3328)
6. ✅ 行距/内边距比之前宽裕
7. ✅ **衬线字体 (Noto Serif SC) 渲染正常**

**如果装机失败**: hap 在 `ohos/.signing/entry-default-signed.hap`, 可能需要重新 build:
```bash
bash tool/ohos/build_hap.sh debug
SIGN_MODE=agc bash tool/ohos/sign_and_install.sh
```

---

## 二、衬线字体方案 (已解决 ✅)

**目标**: 让鸿蒙卡片用 Noto Serif SC 衬线字体, 接近西窗烛/句子控的文学感。

**最终方案**: 方案 A (升级 SDK) — OpenHarmony 6.1-Release SDK (API 23) + `text.FontCollection.getLocalInstance().loadFontSync()`

**解决记录 (2026-06-28)**:
- 下载 OpenHarmony 6.1-Release SDK → `~/sdks/ohos-sdk/sdk/23/` (API 23, 含 getLocalInstance)
- `ohos/build-profile.json5`: compileSdkVersion 20→23, compatibleSdkVersion 保持 20
- `QuoteCard.ets`: `aboutToAppear()` 中 `loadFontSync('NotoSerifSC', $rawfile(...))` + `.fontFamily('NotoSerifSC')` + try-catch 回落
- hap 编译通过 (11.8s) → AGC 签名 → 装机 → 用户真机确认：卡片字体为衬线 ✅

**之前失败原因**: 下载的 OpenHarmony 6.0.0.2-Release 实际是 API 20, 不含 getLocalInstance。OpenHarmony release tag ≠ API level。

**详细研究记录见**: `.slim/deepwork/card-serif-font.md`

---

## 三、待提交的文档更新 (已完成 ✅)

**README.md**: 已更新到 v0.21.2 (最新版本 + 版本条目 + 衬线字体)。
**plan.md**: 标题 v0.21.1 → v0.21.2, 版本日志已含 v0.21.2 条目。

---

## 四、工作树未跟踪文件

```
?? CLAUDE-FABLE-5.md
?? CLAUDE.md
?? 用户需求TODO.md
```

这三个文件一直未纳入版本控制。`用户需求TODO.md` 是用户的需求清单 (2 条), `CLAUDE.md` / `CLAUDE-FABLE-5.md` 是会话相关元文件。需用户决定是否 gitignore 或纳入。

---

## 五、ArkTS 编译修复 (已解决但需注意)

**问题**: `postCardAction(this, { action: 'message', params: {...} })` 报 `arkts-no-untyped-obj-literals`。

**修复**: 定义显式 interface:
```typescript
interface CardRouterAction { action: string; abilityName: string; }
interface CardMessageAction { action: string; event: string; }
```

**注意**: 此问题可能因 hvigor/ets-loader 更新而新出现 — 旧代码 (v0.21.0 之前) 用 `as Record<string, string>` 能编译, 现在不行了。如果未来从 git 历史恢复旧 ArkTS 代码, 需要同步修这个。

---

## 六、版本线回顾

| 版本 | commit | 内容 | 真机验收 |
|------|--------|------|----------|
| v0.20.0 | `e4f20ee` | 鸿蒙卡片视觉改版 + 展示去序号 | ✅ 用户确认 |
| v0.20.1 | `18904a0` | 三栏导航对齐设计系统 (首页/金库/我的) | ✅ 用户确认 |
| v0.21.0 | `be722d2` | 小组件配色 3→6 + 预览屏对齐鸿蒙三档 | ✅ 用户确认 |
| v0.21.2 | `66d7ea7` | 卡片去金印+去模式按钮+墨色调暖+行距内边距+**衬线字体** | ✅ 用户确认 |

---

## 建议的 Skills

- **ohos-dev**: 鸿蒙构建/签名/装机/调试速查 — 装机验收时必用
- **xiao-jinku-design**: 设计系统规范 — 评估卡片视觉时对照
- **deepwork**: 如果继续追衬线字体方案 (复杂多阶段任务)
- **cpf-flutter**: 鸿蒙化 Flutter fork 的已知 bug 和工具链 — SDK 升级时参考
