# 问题总结 — 未完成工作交接

> 生成时间: 2026-06-28
> 当前版本: v0.21.2+66 (已提交 `66d7ea7`, hap 构建成功, 未装机验收)
> 上一验收版本: v0.21.0 (用户真机确认通过)

---

## 一、v0.21.2 真机验收 (阻塞中)

**状态**: hap 已构建 + AGC 签名成功, 装机时设备断开。

**要做的**:
1. 确认 Mate 80 已连接: `export PATH="$HOME/sdks/ohos-sdk/sdk/20/toolchains:$PATH" && hdc list targets`
2. 装机: `cd /Users/qpzm7903/workspace/opensource/my/folio && SIGN_MODE=agc bash tool/ohos/sign_and_install.sh`
3. 拉起 app: `hdc shell aa start -a EntryAbility -b app.folio.quotes -m entry`
4. 回到桌面看卡片, 验证:
   - 左下角无「金」朱印
   - 右下角只有 1 个按钮 (下一句箭头), 无模式切换按钮
   - 点下一句能换句
   - 墨色比之前暖 (paper 主题 ink 从 #1d2a1f → #2a3328)
   - 行距/内边距比之前宽裕
5. 对比西窗烛/句子控卡片, 评估是否还需要进一步调整

**如果装机失败**: hap 在 `ohos/.signing/entry-default-signed.hap`, 可能需要重新 build:
```bash
bash tool/ohos/build_hap.sh debug
SIGN_MODE=agc bash tool/ohos/sign_and_install.sh
```

---

## 二、衬线字体方案 (未解决, 已研究)

**目标**: 让鸿蒙卡片用 Noto Serif SC 衬线字体, 接近西窗烛/句子控的文学感。

**当前状态**: 方案 A (升级 SDK) 失败, 方案 D (接受无衬线+排版改善) 已落地。

**详细研究记录见**: `.slim/deepwork/card-serif-font.md`

### 核心障碍

| 方案 | 结论 | 原因 |
|------|------|------|
| A. 升级 SDK 22+ 用 `getLocalInstance()` | ❌ 失败 | 下载的 OpenHarmony 6.0.0.2-Release 实际是 API 20 (text.d.ts md5 与 SDK 20 一致, 无 getLocalInstance) |
| B. Flutter 渲染文字为图片传卡片 | ❌ 不推荐 | fd 25s 过期 + 失去 Text ellipsis/lineHeight/无障碍, 复杂度不值得 |
| C. 系统衬线字体 | ❌ 不存在 | HarmonyOS 只有 HarmonyOS Sans (无衬线), 无宋体/Source Han Serif |
| D. 接受无衬线 + 排版改善 | ✅ 已执行 | 墨色调暖 + 行距加大 + 内边距加大 |

### 下一步方向 (如果要继续追衬线字体)

1. **找到真正的 API 22+ SDK**: OpenHarmony 版本号 ≠ API level。需要确认哪个 OpenHarmony release 真正包含 `text.FontCollection.getLocalInstance()`。可能需要:
   - 试 HarmonyOS SDK (非 OpenHarmony): `https://repo.huaweicloud.com/harmonyos/os/` (需登录)
   - 试 OpenHarmony 6.1-Release / 6.1.1-Release (librarian 报告说是 API 23/24, 但 6.0.0.2 报告是 API 22 实际是 20, 需验证)
   - 在设备上 `hdc shell ls /system/fonts/` 确认有无衬线字体 (上次因设备断开未完成)

2. **如果找到 API 22+ SDK**:
   - 解压到 `~/sdks/ohos-sdk/sdk/22/` (布局: `{ets,js,native,toolchains,previewer}/oh-uni-package.json` + 顶层 `sdk-pkg.json`)
   - 修 `oh-uni-package.json` 的 `apiVersion` 为 22
   - 建 `~/sdks/hos-sdk/22` 符号链接
   - `ohos/build-profile.json5`: `compileSdkVersion: 22` (保持 `compatibleSdkVersion: 20`)
   - 拷 `assets/fonts/NotoSerifSC-Regular.ttf` 到 `ohos/entry/src/main/resources/rawfile/`
   - `QuoteCard.ets` 加:
     ```typescript
     import { text } from '@kit.ArkGraphics2D';
     // 在 struct QuoteCard 内:
     aboutToAppear() {
       try {
         text.FontCollection.getLocalInstance().loadFontSync(
           'NotoSerifSC', $rawfile('NotoSerifSC-Regular.ttf'));
       } catch (e) { /* 回落系统字体 */ }
     }
     // Text 上加 .fontFamily('NotoSerifSC')
     ```
   - 试构建, 如果 flutter-ohos fork 报 compatibleSdkVersion 冲突, 把 compatibleSdkVersion 也 bump 到 22

3. **如果永远找不到 SDK**: 方案 B (文字转图片) 是唯一剩余路径, 但 oracle 不推荐。建议永久接受方案 D。

---

## 三、待提交的文档更新

**README.md** 还停在 v0.20.1, 需更新到 v0.21.2:
- 加 v0.21.2 条目 (卡片去金印+去模式按钮+墨色调暖+行距内边距)
- 加 v0.21.1 条目 (被合并进 v0.21.2, 可压缩成一行或合并)
- 保留最近 5 版本, 旧版压缩

**plan.md** v0.21.1 条目标题写的是 "v0.21.1" 但实际版本号是 v0.21.2 (跳过了 v0.21.1), 条目内容已更新但标题需核对。

---

## 四、工作树未跟踪文件

```
?? CLAUDE-FABLE-5.md
?? CLAUDE.md
?? 用户需求TODO.md
```

这三个文件一直未纳入版本控制。`用户需求TODO.md` 是用户的需求清单 (2 条), `CLAUDE.md` / `CLAUDE-FABLE-5.md` 是会话相关元文件。需用户决定是否 gitignore 或纳入。

---

## 五、ArkTS 编译修复 (已解决但需注意)

**问题**: `postCardAction(this, { action: 'message', params: {...} })` 报 `arkts-no-untyped-obj-literals`。

**修复**: 定义显式 interface:
```typescript
interface CardRouterAction { action: string; abilityName: string; }
interface CardMessageAction { action: string; event: string; }
```

**注意**: 此问题可能因 hvigor/ets-loader 更新而新出现 — 旧代码 (v0.21.0 之前) 用 `as Record<string, string>` 能编译, 现在不行了。如果未来从 git 历史恢复旧 ArkTS 代码, 需要同步修这个。

---

## 六、版本线回顾

| 版本 | commit | 内容 | 真机验收 |
|------|--------|------|----------|
| v0.20.0 | `e4f20ee` | 鸿蒙卡片视觉改版 + 展示去序号 | ✅ 用户确认 |
| v0.20.1 | `18904a0` | 三栏导航对齐设计系统 (首页/金库/我的) | ✅ 用户确认 |
| v0.21.0 | `be722d2` | 小组件配色 3→6 + 预览屏对齐鸿蒙三档 | ✅ 用户确认 |
| v0.21.2 | `66d7ea7` | 卡片去金印+去模式按钮+墨色调暖+行距内边距 | ⏳ 待装机 |

---

## 建议的 Skills

- **ohos-dev**: 鸿蒙构建/签名/装机/调试速查 — 装机验收时必用
- **xiao-jinku-design**: 设计系统规范 — 评估卡片视觉时对照
- **deepwork**: 如果继续追衬线字体方案 (复杂多阶段任务)
- **cpf-flutter**: 鸿蒙化 Flutter fork 的已知 bug 和工具链 — SDK 升级时参考
