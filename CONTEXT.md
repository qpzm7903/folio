# CONTEXT — folio · 小金库

本文件是项目的领域术语表。只记录术语和含义,不记录实现细节。

## 术语

### 能力开关 (Capability Gate)

"当前运行平台具备某项能力"的布尔判断,集中在一处声明。业务和 UI 层只问
"有没有这个能力",从不问"现在是什么平台"。平台缺位的能力对应一个降级
体验(隐藏入口或换用兜底实现),而不是报错。

### 服务卡片 (Service Widget)

鸿蒙(HarmonyOS)生态中"桌面小组件"的对应概念。与 Android 桌面小组件
(App Widget)目标一致——在桌面常驻展示金句——但属于不同的平台机制,
二者互不复用。

### 纯血鸿蒙 (HarmonyOS NEXT 血统)

指 HarmonyOS 5.0 及之后(含 6.0)不再兼容 Android 应用的系统形态。
对本项目的含义:Android 产物(APK)在这类设备上完全不可用,鸿蒙是
一个独立的目标平台,不是 Android 的变体。

### 金库 (Vault)

用户收藏的全部金句的集合,本项目的核心领域对象容器。

### 金句 (Quote)

用户摘录的一句话,金库中的基本单元,可被标签、搜索、收藏、展示。

### 屏保 (Screensaver / Display)

全屏轮播展示金句的模式,"再次遇见你自己摘下的句子"的主要场景。
